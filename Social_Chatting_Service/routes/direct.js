var express = require('express');
var router = express.Router();
var io = require('socket.io')()

/* GET users listing. */
router.get('/', function(req, res, next) { // Direct 채팅방 입장
  // var 
  // var chatroom_no = 
  // var parter_id = request.

  res.send('respond with a resource'); //
}); 

module.exports = router;
